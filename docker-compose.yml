version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-clear-tab
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      HOST: ${HOST:-0.0.0.0}
      PORT: ${PORT:-3000}
      API_PREFIX: ${API_PREFIX:-/api}
      SWAGGER_ENABLED: ${SWAGGER_ENABLED:-true}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Add other required environment variables here
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {if (r.statusCode !== 200) throw new Error(r.statusCode)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    # Optional: Add resource limits
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

  # Development service with volume mounts (for local development)
  # Uncomment below to use:
  # api-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   container_name: api-clear-tab-dev
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     NODE_ENV: development
  #     HOST: 0.0.0.0
  #     PORT: 3000
  #   volumes:
  #     - ./src:/app/src
  #     - ./node_modules:/app/node_modules
  #   command: pnpm dev
  #   restart: unless-stopped
